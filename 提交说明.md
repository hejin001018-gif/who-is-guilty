# 《疑案追声》AIGC 游戏作业提交说明

## 作品简介

《疑案追声》是一款 HTML5 多角色证词推理游戏。玩家需要在有限回合内审问随机生成的相关人物、搜集证据并提交最终判断。

游戏包含以下 AIGC 机制：

- DeepSeek 案件导演：动态生成案件背景、人物身份、关系网、隐藏真相与证据闭环。
- DeepSeek 角色 AI：分别扮演最多三名人物，根据各自知识边界、私人秘密和对话历史回答玩家。
- 每局随机启用 2～3 名人物，并重新分配姓名、年龄、职业、关系和罪责。
- 未配置 API Key 时自动使用内置案件与本地证词逻辑，仍可完整游玩。

## 运行环境

- Node.js 20 或更高版本
- Chrome、Edge 等现代浏览器

## 快速启动

在本目录打开 PowerShell：

```powershell
npm install
npm start
```

随后访问：

```text
http://127.0.0.1:3000
```

未配置密钥时会自动进入本地模式，不影响基础玩法演示。

## 配置 AI 模型

作业包不包含任何真实密钥。需要体验在线 AI 时，可复制 `.env.example` 为 `.env`，再填写：

```text
GOD_AI_KEY=你的 DeepSeek API Key
AI_1_KEY=你的 DeepSeek API Key
AI_2_KEY=你的 DeepSeek API Key
AI_3_KEY=你的 DeepSeek API Key
```

也可以在 `key.txt` 中使用相同的 `NAME=value` 格式。`key.txt` 和 `.env` 均不会被网页公开。

## 主要文件

- `index.html`：游戏页面结构
- `styles.css`：界面与响应式布局
- `game.js`：前端状态、审讯、搜证和结案交互
- `server.js`：Node 后端、模型代理和安全校验
- `case-engine.js`：案件生成、角色分配与证据逻辑
- `assets/`：游戏图形资源
- `test/`：自动化测试

## 项目检查

```powershell
npm run check
npm test
```

当前版本已通过语法检查和全部自动化测试。

## 安全说明

- 浏览器无法访问 API Key、隐藏身份、未公开证据或结案答案。
- 所有模型调用均由后端代理完成。
- 提交压缩包已排除 `key.txt`、`.env`、`node_modules` 和历史压缩包。
